ctx.req.body là của firebase function parse body 
lỗi đôi khi ở build không thàng công do code lỗi 
nếu loading ở embed admin app load lâu , có thể do lỗi FE 
tạo webhoooks order/create bằng graphql để test thì bỏ field "filter": "type:lookbook"
làm chức năng thay đổi số lượng phần tử của trang thì khi thay đổi giá trị số lượng mà ở trang nào cũng nhảy về trang 1 cho dễ
làm chức năng thay đổi giá trị sort của trang thì khi thay đổi giá trị sort ( asc , desc) mà ở trang nào cũng nhảy về trang 1 cho dễ
làm chức năng xóa phần tử của 1 trang mà ở trang nào thì cũng nhảy về trang 1 cho dễ
webhook order create trả về data product_id khác khi thanh toán bằng thẻ test (Test Order / Bogus gateway) . muốn test thì tao order trong trang admin
8/12/2025 : bug :  url api trong source scripttag phải là link local (link local cũng bị CORS nhưng app.use(cors()) là hết ) , nếu là link tunel cloudflare sẽ dính CORS ( không fix được )
10/12/2025 : cần fix Notifications.js ( source frontend) , push 2 file csv vào nhưng chỉ hiện data 1 file khi push lên backend (đã fix)
14/12/2025 : bug Notifications.js ( source frontend) , khi click xóa file hợp lệ mà convert thành công sang notification
ở DataTable nhưng ở state chứa toàn bộ notifi của tất cả các file không xóa dữ liệu của file đã thêm đó
( mới chỉ xóa trong state hiển thị trong DataTable) => mặc dù xóa trên giao diện đã ẩn đi nhưng khi push lên backend thì vẫn nguyên
(đã fix bằng cách dùng chung 1 state cho cả 2 )
15/12/2025 : ở store front cái popup có làm được chỉ khi click vào foating button thì mới load component hiển thị popup (lazy load)
16/12/2025 : cần sửa liquid theme embed extention thay vi load file scripttag ở mọi trang thì chỉ load ở trang nào chỉ định theo setting , làm lazy load cho displaymanager